# 生日贺卡系统 - 功能测试指南

## 一、环境准备

### 1. 启动服务

```bash
# 后端（在 server/ 目录下）
node src/app.js
# 服务运行在 http://localhost:3000

# 前端（在 admin-web/ 目录下）
npm run dev
# 服务运行在 http://localhost:5173
```

### 2. 默认登录账号

- 用户名：`admin`
- 密码：`admin123`

### 3. 短信发送模式

在 `server/.env` 中配置 `SMS_PROVIDER`：

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| `mock` | 不实际发送，仅打印日志 | 本地开发测试 |
| `carrier` | 通过彩信网关发送 | 生产环境（旧接口） |
| `csp` | 通过5G视信CSP接口发送 | 生产环境（新接口） |

**测试时务必使用 `mock` 模式**，避免误发短信。

---

## 二、主要功能测试方法

### 2.1 仪表盘

**功能**：展示系统概览数据（员工总数、模板数、发送统计、今日生日等）

**测试方法**：
1. 登录后自动进入仪表盘页面
2. 检查各统计卡片数据是否正确
3. 检查月度发送趋势图是否有数据

---

### 2.2 人员管理

#### 查看人员列表
1. 点击左侧菜单「人员管理」→「人员列表」
2. 验证列表显示员工姓名、部门、职位、级别、生日等信息
3. 点击左侧部门树中的子部门，右侧应只显示该部门员工
4. 点击根部门「信阳移动公司工会」，右侧应不显示员工

#### 添加人员
1. 点击「人员管理」→「添加人员」
2. 填写姓名、手机号、生日、部门、级别（employee/manager/management）等
3. 提交后验证列表中出现新员工

#### 批量导入
1. 点击「人员管理」→「批量导入」
2. 下载 Excel 模板
3. 填写员工信息后上传
4. 验证导入结果和错误提示

#### 编辑/删除
1. 在列表中点击编辑按钮修改员工信息
2. 点击删除按钮删除员工（硬删除，同时删除其发送记录）

---

### 2.3 模板管理

#### 查看模板列表
1. 点击左侧菜单「模板管理」
2. 验证5个模板卡片显示封面图
3. 点击「预览」在新窗口查看模板效果

#### 编辑模板
1. 点击模板卡片上的「编辑」按钮
2. 左侧为 HTML 源码编辑区，右侧为实时预览
3. 修改内容后保存

#### 模板匹配规则
| 员工级别 | 匹配模板 | 页数 |
|----------|----------|------|
| management（管理层） | 私宴/荣誉画廊/时光档案高管版 | 7页 |
| manager（经理） | 私宴/荣誉画廊/时光档案高管版 | 7页 |
| employee（普通员工） | 温馨员工版/夜景高管版 | 4页 |

---

### 2.4 祝福语管理

1. 点击左侧菜单「祝福语管理」
2. 添加/编辑/删除祝福语
3. 可设置匹配性别和员工级别

---

### 2.5 发送记录

#### 查看发送记录
1. 点击左侧菜单「发送记录」
2. 查看历史发送记录，包含状态、发送时间等

#### 手动测试发送（核心测试功能）

**这是测试完整发送流程的关键入口**：

1. 在发送记录页面，点击「测试发送」按钮
2. 选择一个员工
3. 系统将执行完整流程：匹配模板 → 生成个性化贺卡HTML → 录制视频 → 发送短信 → 创建记录
4. 在 mock 模式下，短信不会实际发送，但贺卡和视频会真实生成

**API 方式测试**：
```bash
# 获取登录 token
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 测试发送（替换 {employeeId} 和 {token}）
curl -X POST http://localhost:3000/api/records/test-send/{employeeId} \
  -H "Authorization: Bearer {token}"
```

---

### 2.6 查看生成的贺卡

每次发送（或测试发送）后，系统会：
1. 生成个性化 HTML 贺卡文件 → `server/generated-cards/`
2. 录制视频文件 → `server/generated-videos/`
3. 在发送记录中生成贺卡链接

**直接访问贺卡**：
- 发送记录中有预览按钮，点击可在浏览器中查看
- 也可直接访问：`http://localhost:3000/card/{cardId}`
- cardId 是 UUID 格式，在发送记录详情中查看

**查看视频**：
- 视频文件路径在发送记录的 `video_path` 字段中
- 可通过 `http://localhost:3000/videos/{filename}` 访问

---

### 2.7 定时任务（自动发送）

**功能**：每天早上 8:00 自动检查今日生日员工并发送贺卡

**测试方法**：
1. 修改某员工的生日为今天的月和日（如今天是7月8日，设为 `1990-07-08`）
2. 等待次日8:00触发，或手动触发：

```bash
# 手动触发生日检查（需要修改 scheduler.js 中的 cron 表达式为每分钟）
# 或者直接调用 API 测试发送
```

3. 检查发送记录中是否生成了新的记录

---

## 三、贺卡视频生成测试

### 3.1 前置条件

视频录制需要安装 Playwright 浏览器和 ffmpeg：

```bash
# 安装 Playwright Chromium
npx playwright install chromium

# ffmpeg 已内嵌在 Playwright 安装中，路径：
# C:\Users\hezhenyu\AppData\Local\ms-playwright\ffmpeg-1011\ffmpeg-win64.exe
```

### 3.2 测试视频生成

**方法一：通过测试发送**
1. 确保后端服务已启动
2. 在发送记录页面点击「测试发送」，选择一个员工
3. 等待视频生成完成（7页模板约需30-60秒，4页模板约需20-40秒）
4. 在发送记录中点击预览查看视频

**方法二：单独测试视频录制**
```bash
# 通过 API 触发
curl -X POST http://localhost:3000/api/records/test-send/1 \
  -H "Authorization: Bearer {token}"
```

### 3.3 视频文件位置

```
server/generated-videos/
├── card_{uuid}.mp4    # 录制并压缩后的视频
```

### 3.4 常见问题

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 视频全黑 | Playwright 未安装浏览器 | `npx playwright install chromium` |
| 视频无声音 | 音乐文件缺失 | 确认 `server/src/data/music/music.mp3` 存在 |
| 视频录制超时 | 7页模板翻页慢 | 检查模板 JS 是否有 nextPage() 函数 |
| ffmpeg 报错 | ffmpeg 未找到 | 检查 Playwright 安装目录下 ffmpeg 是否存在 |

---

## 四、各 API 快速测试

### 认证
```bash
# 登录
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 员工
```bash
# 获取员工列表
curl http://localhost:3000/api/employees?page=1&pageSize=10 \
  -H "Authorization: Bearer {token}"

# 今日生日员工
curl http://localhost:3000/api/employees/today-birthday \
  -H "Authorization: Bearer {token}"
```

### 模板
```bash
# 模板列表
curl http://localhost:3000/api/templates \
  -H "Authorization: Bearer {token}"

# 模板预览（返回 HTML）
curl http://localhost:3000/api/templates/63/preview \
  -H "Authorization: Bearer {token}"
```

### 发送
```bash
# 测试发送（完整流程）
curl -X POST http://localhost:3000/api/records/test-send/1 \
  -H "Authorization: Bearer {token}"

# 发送记录统计
curl http://localhost:3000/api/records/stats \
  -H "Authorization: Bearer {token}"
```

### 贺卡访问（无需认证）
```bash
# 访问生成的贺卡
curl http://localhost:3000/card/{cardId}
```

### 静态资源（无需认证）
```bash
# 背景音乐
curl -I http://localhost:3000/music/music.mp3

# Logo
curl -I http://localhost:3000/logo/logo.svg

# 模板封面图
curl -I http://localhost:3000/template-previews/template-63-cover.png
```

---

## 五、数据库直接验证

```sql
-- 查看模板列表
SELECT id, name, employee_level, page_count FROM templates;

-- 查看员工级别分布
SELECT level, COUNT(*) FROM employees GROUP BY level;

-- 查看发送记录
SELECT id, employee_id, status, send_type, card_url FROM send_records ORDER BY id DESC LIMIT 10;

-- 查看今日生日员工
SELECT id, name, birthday, level FROM employees WHERE MONTH(birthday) = MONTH(NOW()) AND DAY(birthday) = DAY(NOW());
```

---

## 六、模板重新导入

如果修改了 `server/src/data/` 中的模板文件，需要重新导入：

```bash
cd server
node import-new-templates.js
```

注意：此操作会清空所有现有模板并重新导入，关联的发送记录 template_id 会被置空。
